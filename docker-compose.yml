services:
  web:
    build: .
    ports:
      - "80:80"
    volumes:
      - storage:/rails/storage
    environment:
      - RAILS_MASTER_KEY=${RAILS_MASTER_KEY}
    restart: unless-stopped

volumes:
  storage:
